# Exclude root node_modules (only concurrently)
/node_modules

# Exclude Frontend files (built separately)
/Frontend/node_modules
/Frontend/dist

# Environment files (injected via Vercel dashboard)
/.env
/.env.*
